# 方法论：面向 IPNM1 的物理约束速度场-传导率预测模型

## 1. 研究问题与总体思路

传统孔网模型在进行单相流计算时，通常需要为每一条喉道或局部子结构给定水力传导率。教师论文中的改进孔网模型（improved pore network model, IPNM）通过局部 LBM 计算或几何修正来改进喉道传导率，从而提高孔网尺度流动模拟的准确性。但是，若模型只给出一个标量传导率 \(g\)，则局部速度分布信息会被压缩掉。对于后续传质、弥散、反应流或多相扩展而言，速度场 \(u(y,z)\) 本身比单一标量更有信息量。

因此，本研究先聚焦 IPNM1，提出一个“速度场优先”的深度学习代理模型。核心思想不是直接从截面几何拟合传导率，而是学习如下映射：

\[
\mathcal{G}(y,z)\ \longrightarrow\ \hat{u}(y,z)\ \longrightarrow\ \hat{q}\ \longrightarrow\ \hat{g},
\]

其中 \(\mathcal{G}(y,z)\) 表示喉道局部截面几何，\(\hat{u}(y,z)\) 是模型预测的轴向速度场，\(\hat{q}\) 是由速度场积分得到的局部流量，\(\hat{g}\) 是进一步换算得到的 IPNM1 局部传导率。

这一设计有三个直接动机。

第一，速度场是比传导率更丰富的物理中间量。标量传导率只反映截面的整体导流能力，而速度场保留了孔隙内部的非均匀流动结构。

第二，传导率可以由速度场通过物理积分得到。因此，如果模型能够准确预测速度场，就可以在不额外训练传导率回归器的情况下得到 \(q\) 和 \(g\)。

第三，直接拟合传导率容易把几何、数值模拟误差和表格标签误差混在一起。相比之下，速度场预测提供了一个可解释的物理中间层，使模型更接近 PINN/physics-guided learning 的思想。

本研究当前只声明 IPNM1。原因是当前用于训练速度场预测器的是预处理后的单截面 HDF5 数据集，具有截面几何、速度场和样本索引；局部传导率标签则通过样本索引与教师表格文件匹配得到。该数据组织尚未完整引入 IPNM2 所需的 parent throat/sub-throat 分组关系、串联等效传导率聚合和网络尺度求解。因此，本文先建立并验证局部截面层面的 IPNM1 速度-传导率代理模型，后续再扩展到 IPNM2。

## 2. 数据表示与几何输入

### 2.1 原始模拟输出与 HDF5 训练集构建

本研究中用于速度场监督训练的 `dataset_all_32.h5` 并不是训练好的模型文件，而是由 `E:\mhw\1\pore\data` 中的原始模拟输出预处理得到的 HDF5 数据集。对应的预处理脚本为：

```text
E:\mhw\1\pore\code\build_hdf5_dataset.py
```

原始数据按岩样编号组织在 `data/1` 到 `data/6` 中。每个岩样目录包含若干 `OUT_<id>_<step>.dat` 文件和 `conductivity.dat` 等表格文件。其中 `OUT_<id>_<step>.dat` 是局部流动模拟输出，包含三维网格坐标、速度分量和固体/孔隙标记。预处理脚本首先扫描每个岩样目录，对同一个 `id` 选择最大 `step` 的 `OUT` 文件作为该局部样本的稳态速度场来源。

对每个 `OUT` 文件，脚本读取 Tecplot 格式中的网格尺寸 \(I,J,K\)，并抽取指定的 \(x\)-方向切片。当前数据集采用：

```text
x_layer = 1
```

因此每个三维局部模拟结果被转换为一个二维 \(y-z\) 截面。该步骤得到原始二维孔隙掩膜 \(M_0(y,z)\) 和原始轴向速度场 \(u_0(y,z)\)。其中孔隙区域由壁面/固体标记确定，固体区域速度被置为零。

### 2.2 连通孔隙区域提取与样本筛选

预处理并不是简单使用固定滑窗切块，而是先在二维孔隙掩膜上进行连通域分析。脚本对孔隙区域使用 8 邻域连通性，提取每个独立孔隙连通区域，并围绕完整连通域生成候选 patch。这样可以避免一个局部喉道截面被滑窗任意切断，使样本更接近物理上完整的局部流动单元。

候选 patch 需要经过多级筛选：

1. 连通域边界框不能超过原始 patch 尺寸；
2. 原始孔隙比例必须位于设定范围内；
3. 孔隙不能主要贴在 patch 边界；
4. 质心重对齐之后仍需满足边界和中心区域有效性；
5. 归一化之后的孔隙比例不能过低。

这些筛选步骤的作用是剔除过小、被截断或主要位于边缘的无效局部样本，保证训练样本具有明确的几何中心和有效流动区域。

### 2.3 质心重对齐与尺度归一化

对通过筛选的局部 patch，首先计算孔隙区域质心：

\[
(y_c,z_c)
=
\frac{1}{|\Omega_p|}
\sum_{(y,z)\in\Omega_p}
(y,z),
\]

然后将孔隙质心平移到 patch 中心。该步骤减少了同一几何结构因位置偏移造成的学习难度，使卷积网络更关注形状与边界，而不是绝对位置。

接着，脚本根据孔隙面积计算等效半径：

\[
R_A=\sqrt{\frac{A}{\pi}},
\]

其中 \(A=\sum M(y,z)\) 是孔隙像素面积。为了将不同尺寸的局部喉道映射到统一输入域，几何被缩放到目标等效半径 \(R_1\)。当前数据集属性显示：

```text
target_R1 = 10.0
raw_patch = 64
patch_size = 64
```

尺度因子定义为：

\[
s=\frac{R_1}{R_A}.
\]

脚本中还设置了缩放范围约束，避免极端小孔或大孔导致过度缩放。几何掩膜使用最近邻插值缩放，速度场使用双线性插值缩放，然后再中心裁剪或补零到统一的 \(64\times64\) 尺寸。

### 2.4 速度归一化与物理尺度恢复

由于不同喉道截面尺寸差异较大，如果直接学习原始速度 \(u_0\)，模型需要同时学习几何形状效应和绝对尺度效应。预处理脚本采用 Hagen-Poiseuille 启发的速度尺度关系：

\[
u \propto R^2.
\]

当原始几何被缩放 \(s=R_1/R_A\) 后，训练集中存储的归一化速度为：

\[
u_{\mathrm{norm}}(y,z)
=
u_{\mathrm{raw}}(y,z)s^2.
\]

因此，模型训练目标 `Y` 是归一化后的轴向速度场，而不是未处理的原始速度。推理后若要恢复物理一致的速度，需要执行反归一化：

\[
u_{\mathrm{raw}}(y,z)
=
\frac{u_{\mathrm{norm}}(y,z)}{s^2}.
\]

此外，由于截面面积在空间缩放中也发生变化，在由速度场积分得到流量时，还需要进行面积尺度恢复。当前 \(y-z\) 截面中，面积随尺度因子按 \(s^2\) 变化，因此后处理时对积分流量进行：

\[
q_{\mathrm{raw}}
=
\frac{q_{\mathrm{norm-int}}}{s^2}.
\]

也就是说，从模型输出到最终流量，需要先恢复速度幅值，再恢复面积尺度。这一点是后续 \(q/g\) 后处理能够与教师表格一致的关键。

### 2.5 HDF5 文件内容

最终得到的 `dataset_all_32.h5` 是一个预处理后的训练数据集。`pore` 和 `cup` 目录中的该文件内容一致，关键规格如下：

```text
X: (1728, 3, 64, 64)
Y: (1728, 1, 64, 64)
channel_order: mask,dist,eta_map
rock_types: 1,2,3,4,5,6
target_R1: 10.0
raw_patch: 64
patch_size: 64
```

其中 `X` 是输入几何张量，`Y` 是归一化速度场标签。额外元数据包括 `rock_type`、`local_id`、`global_id`、`step`、`R0_RA`、`RI`、`eta`、`scale_s`、`q0` 和 `q_norm` 等。`scale_s` 保存每个样本的几何缩放因子，是后续速度和面积恢复所必需的物理量。

当前 1728 个样本在 6 类岩样中的数量分别为：

\[
700,\ 119,\ 368,\ 268,\ 221,\ 52.
\]

需要再次强调，`dataset_all_32.h5` 是训练数据集，模型训练后的权重保存在各实验目录的 `best.pt` 中，模型推理输出保存在 `predictions.npz` 中。

### 2.6 模型输入通道

设一个局部喉道截面位于 \(y-z\) 平面，流动方向为 \(x\)。对第 \(i\) 个样本，其输入几何表示为多通道张量：

\[
\mathbf{X}_i \in \mathbb{R}^{C \times H \times W},
\]

其中 \(H\) 和 \(W\) 是离散截面的高度与宽度，\(C\) 是几何通道数。需要说明的是，`dataset_all_32.h5` 不是训练好的模型文件，而是由原始模拟输出预处理得到的 HDF5 训练数据集。模型权重保存在各实验目录的 `best.pt` 中，推理结果保存在 `predictions.npz` 中。当前 HDF5 数据集中，每个样本包含：

- 孔隙掩膜 \(M_i(y,z)\)，其中 \(M=1\) 表示孔隙，\(M=0\) 表示固体；
- 距离变换特征，用于表征像素到固体边界的局部距离；
- 形状相关特征，例如内切半径、等效半径和形状因子等派生量；
- 局部孔隙率、左右累积阻力近似图和坐标特征等辅助几何描述。

孔隙掩膜是最核心的物理约束，因为速度损失和流量积分都只在孔隙区域内计算。固体区域不参与速度误差统计，也不参与流量积分。

为了避免模型依赖岩样类别，最终模型不使用岩样分类、不使用 rock-type channel，也不使用 FiLM 岩样条件化。所有样本在同一模型中训练，即学习统一的几何到流动映射：

\[
f_\theta:\mathbf{X}_i \mapsto \hat{\mathbf{U}}_i,
\]

其中 \(\hat{\mathbf{U}}_i\in\mathbb{R}^{1\times H\times W}\) 是预测速度场。这样做的目的是证明：在 IPNM1 局部截面问题中，截面几何本身足以提供主要流动信息，不必先把岩样划分为不同类别再分别建模。

## 3. Res-IPNM1-FlowNet 网络结构

本文最终采用 ResNet-style encoder-decoder 作为主干网络，并命名为 Res-IPNM1-FlowNet。给定几何输入 \(\mathbf{X}\)，编码器逐层提取多尺度几何特征，解码器再将特征恢复到原始截面分辨率，输出稠密速度场：

\[
\hat{\mathbf{U}} = f_\theta(\mathbf{X}).
\]

网络的核心结构包括：

1. 初始卷积层，将多通道几何输入映射到基础特征空间；
2. 多层残差编码模块，通过下采样获得大尺度几何上下文；
3. 多层解码模块，通过上采样恢复空间分辨率；
4. 跳跃连接，将浅层边界信息与深层语义特征融合；
5. 单通道输出层，生成轴向速度场。

残差结构的作用是稳定训练，并减少深层卷积网络中的梯度退化问题。编码-解码结构适合本任务，是因为输出不是一个标量，而是与输入截面同尺寸的速度分布。相比直接回归 \(q\) 或 \(g\)，该结构保留了空间分布；相比早期四分支 IPNM1-FlowNet，ResNet encoder-decoder 更简洁，且在统一岩样训练下更加稳定。

预测后，模型输出会与孔隙掩膜相乘：

\[
\hat{u}_p(y,z)=\hat{u}(y,z)M(y,z),
\]

从而保证固体区域不对后续流量积分产生贡献。

## 4. 速度场监督损失

令真实 LBM 速度场为 \(u(y,z)\)，模型预测速度场为 \(\hat{u}(y,z)\)，孔隙区域为：

\[
\Omega_p = \{(y,z)\mid M(y,z)=1\}.
\]

速度场监督项只在孔隙像素上计算。可写为：

\[
\mathcal{L}_{\mathrm{field}}
=
\frac{1}{|\Omega_p|}
\sum_{(y,z)\in\Omega_p}
\ell\left(\hat{u}(y,z),u(y,z)\right),
\]

其中 \(\ell(\cdot)\) 可取 MAE、Smooth L1 或与工程代码一致的像素级误差形式。该项保证模型首先学习局部速度分布，而不是只学习一个整体流量。

这一损失对应论文中的第一个目标：预测速度场。其评估指标是孔隙区域像素级 \(R^2\)：

\[
R^2_u
=
1-
\frac{
\sum_{(i,y,z)\in\Omega_p}
\left(\hat{u}_i(y,z)-u_i(y,z)\right)^2
}{
\sum_{(i,y,z)\in\Omega_p}
\left(u_i(y,z)-\bar{u}\right)^2
},
\]

其中 \(\bar{u}\) 是所有参与统计的真实孔隙速度像素的均值。当前主模型的速度场 \(R^2\) 为：

\[
R^2_u = 0.9875\pm0.0006.
\]

## 5. IPNM1 积分流量约束

仅有像素级速度损失并不一定能保证整体流量准确。原因是速度场中局部小误差可能在积分后累积为明显的 \(q\) 偏差。因此，我们引入 IPNM1 积分流量约束，使模型同时满足局部速度精度和整体守恒量精度。

对二维截面，局部体积流量可以由速度场在孔隙区域积分得到：

\[
q
=
\int_{\Omega_p} u(y,z)\, \mathrm{d}A.
\]

在离散像素网格上，该积分近似为：

\[
q
\approx
\sum_{(y,z)\in\Omega_p}
u(y,z)\Delta A,
\]

其中 \(\Delta A=\Delta y\Delta z\)。在代码实现中，速度场经过归一化恢复和面积尺度恢复后，再进行求和积分。对应的预测流量为：

\[
\hat{q}
\approx
\sum_{(y,z)\in\Omega_p}
\hat{u}(y,z)\Delta A.
\]

为了让模型的速度场在积分意义上接近真实流量，引入相对流量误差损失：

\[
\mathcal{L}_{\mathrm{flux}}
=
\frac{|\hat{q}-q|}{|q|+\varepsilon},
\]

其中 \(\varepsilon\) 是防止分母为零的小常数。最终训练目标为：

\[
\mathcal{L}
=
\mathcal{L}_{\mathrm{field}}
\lambda_{\mathrm{flux}}\mathcal{L}_{\mathrm{flux}}.
\]

本研究通过消融实验选择：

\[
\lambda_{\mathrm{flux}}=0.10.
\]

这个权重的意义是：速度场仍然是主监督目标，积分流量是物理一致性约束。若 \(\lambda_{\mathrm{flux}}\) 过小，模型可能只优化局部像素误差，导致 \(q/g\) 稍弱；若 \(\lambda_{\mathrm{flux}}\) 过大，模型可能牺牲局部速度分布去满足整体流量，从而降低速度场质量。

消融结果支持这一判断。无 flux 约束时，速度场 \(R^2\) 仍有 0.9879，但表格传导率误差为 3.01%。当 \(\lambda_{\mathrm{flux}}=0.10\) 时，表格传导率误差降至 2.05%，且速度场 \(R^2\) 保持在 0.9880。进一步增大到 \(\lambda_{\mathrm{flux}}=1.00\) 时，传导率没有继续明显改善，但速度场 \(R^2\) 降至 0.9795。

因此，\(\lambda_{\mathrm{flux}}=0.10\) 是当前数据上的折中最优选择。

## 6. 由流量恢复 IPNM1 局部传导率

在 IPNM1 中，局部喉道或截面的水力传导率本质上描述流量对驱动力的响应。一般可以写成：

\[
g = \frac{q}{\Delta P},
\]

其中 \(\Delta P\) 是局部压力差。对于教师数据中采用的 LBM body-force 单位体系，后处理脚本使用与原始表格一致的换算形式：

\[
g
=
\frac{q\,\mu_{\mathrm{target}}}
{\rho a_x L \mu_{\mathrm{LBM}}},
\]

其中 \(\rho\) 是流体密度，\(a_x\) 是沿流向的体加速度，\(L\) 是局部段长，\(\mu_{\mathrm{LBM}}\) 是 LBM 模拟中的黏度，\(\mu_{\mathrm{target}}\) 是目标黏度。当前实验采用：

\[
\rho=1,\quad
a_x=10^{-4},\quad
L=4,\quad
\mu_{\mathrm{LBM}}=0.5,\quad
\mu_{\mathrm{target}}=1.
\]

因此，模型预测传导率为：

\[
\hat{g}
=
\frac{\hat{q}\,\mu_{\mathrm{target}}}
{\rho a_x L \mu_{\mathrm{LBM}}}.
\]

需要强调的是，当前这里的 \(g\) 是 IPNM1 局部截面/局部喉道传导率后处理结果，而不是 IPNM2 网络级等效传导率。IPNM2 需要将多个 sub-throat 的传导率按串联关系聚合为完整 throat 传导率，并进一步回填到 PNM 网络中求解整体流动。该部分属于后续工作。

## 7. 评价指标

本文使用三类指标评价模型。

第一类是速度场指标。速度场 \(R^2\) 以真实 LBM 速度场 \(Y\) 为基准，并且只在孔隙区域统计：

\[
R^2_u
=
1-
\frac{\sum(\hat{u}-u)^2}{\sum(u-\bar{u})^2}.
\]

该指标回答：模型是否真的预测出了速度分布。

第二类是流量指标。预测速度场和真实速度场分别积分得到 \(\hat{q}\) 和 \(q\)。流量相对误差为：

\[
\mathrm{RE}_q
=
\frac{|\hat{q}-q|}{|q|+\varepsilon}.
\]

同时计算 \(q\) 的 \(R^2\)：

\[
R^2_q
=
1-
\frac{\sum(\hat{q}_i-q_i)^2}{\sum(q_i-\bar{q})^2}.
\]

第三类是传导率指标。预测传导率 \(\hat{g}\) 与两类参考值比较：

1. 与真实速度场积分得到的 \(g_{\mathrm{true\ velocity}}\) 比较；
2. 与教师表格中的 \(g_{\mathrm{table}}\) 比较。

主结果中更强调 \(g_{\mathrm{table}}\)，因为它对应教师 IPNM1 数据中的传导率标签。传导率相对误差为：

\[
\mathrm{RE}_g
=
\frac{|\hat{g}-g|}{|g|+\varepsilon},
\]

传导率 \(R^2\) 为：

\[
R^2_g
=
1-
\frac{\sum(\hat{g}_i-g_i)^2}{\sum(g_i-\bar{g})^2}.
\]

当前主模型相对教师表格传导率的结果为：

\[
\mathrm{RE}_{g,\mathrm{table}}
=
2.07\%\pm0.02\%,
\]

\[
R^2_{g,\mathrm{table}}
=
0.9980\pm0.0001.
\]

## 8. 与直接标量拟合的区别

为了验证速度场优先路线的必要性，本文设置了直接标量拟合 baseline。该 baseline 直接学习：

\[
\mathbf{X}\rightarrow q
\quad\text{或}\quad
\mathbf{X}\rightarrow g.
\]

若只看 \(q_{\mathrm{true}}\)，直接拟合可以达到 2.22% 左右的平均相对误差，说明几何到整体流量的标量关系是可学习的。但是这种方法没有速度场输出，因此不能服务于后续传质和速度分布分析。

更重要的是，直接拟合表格 \(q_{\mathrm{table}}/g_{\mathrm{table}}\) 不稳定，测试误差显著增大。这说明表格标量中包含几何、数值换算和数据表波动等混合因素。直接把这些因素压缩进一个标量回归目标，会削弱模型的物理可解释性。

相比之下，Res-IPNM1-FlowNet 的逻辑是：

\[
\mathbf{X}
\xrightarrow{f_\theta}
\hat{u}(y,z)
\xrightarrow{\int_{\Omega_p}}
\hat{q}
\xrightarrow{\mathrm{IPNM1\ conversion}}
\hat{g}.
\]

该路径把模型学习目标和物理后处理分开。神经网络负责学习复杂几何到速度分布的映射；IPNM1 积分和换算负责从速度场得到传导率。这样既保持了深度学习的表达能力，又保留了孔网模型的物理解释。

## 9. 教师 300x200x200 数据与 IPNM1 适用性

教师提供的 `300x200x200_data` 包含 Bead、Benth、Berea_ICL 和 Font18 等岩样的局部截面文件，以及 PNM_simulation 中的传导率表。数据审计显示，有效截面数量分别为：

\[
\text{Bead}:1255,\quad
\text{Benth}:1719,\quad
\text{Berea\_ICL}:1560,\quad
\text{Font18}:1267.
\]

所有有效截面都能通过 local_id 匹配到对应传导率行。这说明教师数据可以支持局部 IPNM1 截面-传导率建模。同时，不同岩样的孔隙像素数分布差异明显，例如 Bead 的中位孔隙像素数为 126，而 Benth、Berea_ICL 和 Font18 分别约为 33、29 和 34。这种差异说明几何分布跨岩样变化较大，也进一步支持采用统一几何驱动模型，而不是人工按岩样分类处理。

需要注意的是，传导率表中还包含部分 disabled 或无效截面对应的行，因此在构建训练或评价样本时必须先进行有效截面筛选。当前 IPNM1 实验只使用具有有效几何、速度场或可匹配传导率标签的样本。

## 10. 方法边界与后续 IPNM2 扩展

本文方法当前解决的是 IPNM1 局部截面问题。它证明了三件事：

1. 统一岩样模型可以从几何预测局部速度场；
2. 预测速度场可以通过积分得到准确的 \(q\)；
3. 由 \(q\) 换算得到的局部传导率 \(g\) 与教师表格高度一致。

但是，IPNM2 还需要额外步骤。若一个完整喉道由多个 sub-throat 组成，则需要将每个 sub-throat 的局部传导率 \(g_k\) 按串联关系聚合：

\[
\frac{1}{g_{\mathrm{throat}}}
=
\sum_{k=1}^{K}
\frac{1}{g_k},
\]

或在包含长度权重的情况下采用对应的等效阻力形式。然后，将聚合后的 throat conductance 回填到 PNM 网络边上，求解节点压力和网络总流量。只有完成这一层网络级求解，才能称为完整 IPNM2。

因此，本文先将贡献限定为 IPNM1：建立速度场-流量-传导率的一致代理模型。该模型为后续 IPNM2 提供局部 sub-throat 传导率预测模块，也为传质和反应流扩展保留速度场输入。

## 11. 方法论小结

Res-IPNM1-FlowNet 的核心方法论可以概括为：用统一深度模型预测局部速度场，用 IPNM1 物理积分恢复流量和传导率。与直接拟合传导率相比，该方法多保留了速度分布；与纯数值模拟相比，该方法具有更高推理效率；与按岩样分类建模相比，该方法更简洁，也更适合后续推广到不同岩样和更复杂孔网结构。

从实验结果看，模型速度场 \(R^2\) 达到 \(0.9875\pm0.0006\)，表格传导率误差为 \(2.07\%\pm0.02\%\)，说明该方法在保持速度场信息的同时，也能准确恢复 IPNM1 局部传导率。这为后续 IPNM2 网络级扩展提供了可解释、可计算且物理一致的局部代理模块。
