# 方法：物理约束的 IPNM1 速度场到传导率代理模型

## 局部孔网传导率建模问题

单相流孔网模拟的核心之一，是为每条喉道或局部流动单元确定可靠的水力传导率。传统改进孔网模型通常通过局部数值模拟或经验修正获得传导率，但这种做法在大规模孔网中会带来较高计算成本。同时，若只保留传导率这一标量，局部速度分布会被压缩掉，从而限制后续传质、弥散和反应流分析。

本研究将 IPNM1 局部截面问题表述为一个物理约束的几何到流动映射问题。给定喉道局部截面几何 \(\mathcal{G}(y,z)\)，模型首先预测轴向速度场 \(\hat{u}(y,z)\)，然后通过截面积分得到局部流量 \(\hat{q}\)，最后根据 IPNM1 的局部传导率定义得到 \(\hat{g}\)。整体链条为

\[
\mathcal{G}(y,z)
\rightarrow
\hat{u}(y,z)
\rightarrow
\hat{q}
\rightarrow
\hat{g}.
\]

这一设计的关键在于，神经网络不直接学习最终传导率，而是学习具有明确物理含义的速度场中间量。传导率由速度场经过积分和物理换算得到。因此，模型既能输出局部速度分布，又能恢复孔网模型所需的传导率。

本文当前只声明 IPNM1。原因是现有监督数据对应单个局部截面，能够支持截面速度场、局部流量和局部传导率的学习与验证；而 IPNM2 还需要明确的子喉道与完整喉道分组关系、串联传导率聚合以及网络尺度压力求解。本文方法因此先作为 IPNM1 局部代理模型建立，后续可作为 IPNM2 中子喉道传导率预测模块。

## 原始流动结果到监督样本的构建

监督数据由多类岩样的局部流动模拟结果构建。每个局部样本包含三维网格中的孔隙/固体标记和速度分量。为了将三维局部流动结果转化为适合 IPNM1 局部截面学习的样本，首先在流动方向上选取固定位置的二维 \(y-z\) 截面，并提取该截面的孔隙掩膜 \(M_0(y,z)\) 与轴向速度 \(u_0(y,z)\)。其中孔隙区域定义为

\[
\Omega_p=\{(y,z)\mid M_0(y,z)=1\}.
\]

为了避免任意滑动窗口切断一个完整局部流动单元，样本构建采用连通孔隙区域为基础。具体而言，先在二维孔隙掩膜中识别连通孔隙域，再围绕每个完整连通域形成候选局部截面。候选样本需要满足孔隙比例、边界接触、中心区域有效性和连通完整性等条件。这样做可以减少无效边缘样本和过小孔隙样本对训练的干扰，使每个监督样本更接近物理上完整的局部喉道截面。

对于通过筛选的局部截面，进一步进行质心重对齐。设孔隙质心为

\[
(y_c,z_c)
=
\frac{1}{|\Omega_p|}
\sum_{(y,z)\in\Omega_p}(y,z),
\]

则将孔隙质心平移至截面中心。该步骤减弱了绝对位置偏移对学习的影响，使模型更关注孔隙边界形状、孔径尺度和局部空间结构。

## 尺度归一化与速度恢复关系

不同局部喉道截面的绝对尺寸差异较大。若直接学习原始速度场，模型需要同时处理几何形状差异和绝对尺度差异。为降低学习难度，并使不同尺寸截面位于统一输入域，本文采用等效半径驱动的尺度归一化。

对一个局部截面，孔隙面积为

\[
A=\sum_{(y,z)} M_0(y,z),
\]

其等效面积半径定义为

\[
R_A=\sqrt{\frac{A}{\pi}}.
\]

令目标等效半径为 \(R_1\)，则几何缩放因子为

\[
s=\frac{R_1}{R_A}.
\]

在本研究数据中，所有局部截面被缩放到统一的目标半径，并重采样到相同空间分辨率。孔隙掩膜采用保持类别边界的方式缩放，速度场采用连续插值缩放。缩放后得到归一化孔隙掩膜 \(M(y,z)\) 和归一化几何描述。

速度幅值的归一化遵循 Hagen-Poiseuille 启发的尺度关系。对于层流管道流动，特征速度随半径平方变化，即

\[
u \propto R^2.
\]

因此，当几何尺度由 \(R_A\) 映射到 \(R_1\) 时，训练目标速度定义为

\[
u_{\mathrm{norm}}(y,z)
=
u_{\mathrm{raw}}(y,z)s^2.
\]

模型学习的是归一化速度场。推理后，为恢复与原始局部几何一致的速度量纲，需要反变换：

\[
\hat{u}_{\mathrm{raw}}(y,z)
=
\frac{\hat{u}_{\mathrm{norm}}(y,z)}{s^2}.
\]

由于截面面积在二维空间缩放中随 \(s^2\) 改变，由速度积分得到流量时还需要面积恢复。若在归一化截面上积分得到 \(\hat{q}_{\mathrm{norm-int}}\)，则恢复到原始截面尺度的流量为

\[
\hat{q}_{\mathrm{raw}}
=
\frac{\hat{q}_{\mathrm{norm-int}}}{s^2}.
\]

因此，从模型输出到物理流量包含两个恢复步骤：先恢复速度幅值，再恢复截面面积。二者共同保证预测流量与原始局部几何下的流动响应一致。

## 几何表征与统一岩样学习

每个局部截面被表示为多通道几何张量。主要几何信息包括孔隙掩膜、到固体边界的距离场以及截面形状因子。孔隙掩膜定义可流动区域；距离场描述孔隙内部位置到壁面的相对远近；形状因子提供截面整体形态信息。形式上，输入可写为

\[
\mathbf{X}
=
\left[
M(y,z),\ D(y,z),\ E(y,z)
\right],
\]

其中 \(M\) 为孔隙掩膜，\(D\) 为距离场，\(E\) 为由截面形状参数构成的空间描述。

最终模型采用统一岩样学习策略。所有岩样样本共同用于训练同一个模型，不再先进行岩样分类，也不引入岩样条件调制。其原因是本文关注的是局部截面几何到局部速度场的物理映射；若几何表征足够充分，模型应主要依据孔隙形态、孔径尺度和边界位置推断速度分布，而不是依赖预先给定的岩样类别。该设定也使模型更适合作为后续孔网模型中的通用局部代理模块。

## 残差编码-解码速度场预测器

本文提出的 Res-IPNM1-FlowNet 采用残差编码-解码结构预测局部轴向速度场。给定几何输入 \(\mathbf{X}\)，模型输出归一化速度场

\[
\hat{\mathbf{U}}_{\mathrm{norm}}
=
f_\theta(\mathbf{X}),
\]

其中 \(f_\theta\) 表示待学习的非线性映射。

编码部分逐层提取局部边界、孔径尺度和更大范围的几何上下文；解码部分将多尺度特征恢复到原始截面分辨率，生成与输入截面同尺寸的速度场。残差连接用于稳定训练并促进多层特征传播。由于任务目标是稠密速度场而不是单个标量，编码-解码结构比纯标量回归更符合问题形式。

预测速度场在后处理中与孔隙掩膜相乘：

\[
\hat{u}(y,z)
=
\hat{u}_{\mathrm{norm}}(y,z)M(y,z),
\]

以保证固体区域不参与速度误差、流量积分和传导率计算。

## 速度场监督与物理积分约束

模型训练目标由两部分组成：局部速度场误差和整体流量一致性误差。第一部分约束每个孔隙像素上的速度预测。设真实归一化速度场为 \(u_{\mathrm{norm}}(y,z)\)，预测速度场为 \(\hat{u}_{\mathrm{norm}}(y,z)\)，速度场损失定义为

\[
\mathcal{L}_{\mathrm{field}}
=
\frac{1}{|\Omega_p|}
\sum_{(y,z)\in\Omega_p}
\ell\left(
\hat{u}_{\mathrm{norm}}(y,z),
u_{\mathrm{norm}}(y,z)
\right),
\]

其中 \(\ell(\cdot)\) 表示像素级误差度量。该项保证模型学习局部速度分布。

第二部分约束由速度场积分得到的整体流量。归一化截面上的预测流量为

\[
\hat{q}
=
\sum_{(y,z)\in\Omega_p}
\hat{u}_{\mathrm{norm}}(y,z)\Delta A,
\]

真实流量为

\[
q
=
\sum_{(y,z)\in\Omega_p}
u_{\mathrm{norm}}(y,z)\Delta A.
\]

相对流量约束定义为

\[
\mathcal{L}_{\mathrm{flux}}
=
\frac{|\hat{q}-q|}{|q|+\varepsilon},
\]

其中 \(\varepsilon\) 为稳定项。最终损失为

\[
\mathcal{L}
=
\mathcal{L}_{\mathrm{field}}
\lambda_{\mathrm{flux}}\mathcal{L}_{\mathrm{flux}}.
\]

该损失体现了本文方法的物理推理：速度场是模型直接学习对象，流量是由速度场积分得到的守恒量。仅优化局部速度误差可能导致积分流量偏差，而仅优化流量又可能损失局部速度分布。因此，适中的积分流量约束可以在局部精度与整体物理一致性之间取得平衡。

消融实验表明，当 \(\lambda_{\mathrm{flux}}=0.10\) 时，模型在保持高速度场精度的同时获得最优传导率恢复效果。因此，本文将其作为最终设置。

## 从速度场到 IPNM1 传导率

模型预测得到局部速度场后，首先恢复原始速度尺度：

\[
\hat{u}_{\mathrm{raw}}(y,z)
=
\frac{\hat{u}_{\mathrm{norm}}(y,z)}{s^2}.
\]

随后在原始面积尺度下计算局部流量：

\[
\hat{q}_{\mathrm{raw}}
=
\int_{\Omega_p}
\hat{u}_{\mathrm{raw}}(y,z)\,\mathrm{d}A.
\]

在离散截面上，该式近似为

\[
\hat{q}_{\mathrm{raw}}
\approx
\sum_{(y,z)\in\Omega_p}
\hat{u}_{\mathrm{raw}}(y,z)\Delta A.
\]

局部传导率反映流量对驱动力的响应。对于压力驱动形式，可写为

\[
g=\frac{q}{\Delta P}.
\]

在本研究采用的局部 LBM 驱动体系下，与教师数据一致的传导率换算写为

\[
\hat{g}
=
\frac{
\hat{q}_{\mathrm{raw}}\mu
}{
\rho a_x L\mu_{\mathrm{LBM}}
},
\]

其中 \(\rho\) 为流体密度，\(a_x\) 为流向体加速度，\(L\) 为局部段长，\(\mu_{\mathrm{LBM}}\) 为局部 LBM 黏度，\(\mu\) 为目标黏度。该式将速度场预测结果转化为 IPNM1 所需的局部传导率。

需要强调的是，此处得到的是局部截面或局部喉道单元的 IPNM1 传导率。完整 IPNM2 还需要将多个子喉道传导率聚合为喉道传导率，再进入网络尺度压力和流量求解。

## 评价基准与误差定义

本文从速度场、流量和传导率三个层次评价模型。

速度场评价以真实局部 LBM 速度场为基准，只在孔隙区域计算像素级 \(R^2\)：

\[
R_u^2
=
1-
\frac{
\sum(\hat{u}-u)^2
}{
\sum(u-\bar{u})^2
}.
\]

该指标回答模型是否真正恢复了局部速度分布。

流量评价比较预测速度场积分结果 \(\hat{q}\) 与真实速度场积分结果 \(q\)。相对误差为

\[
\mathrm{RE}_q
=
\frac{|\hat{q}-q|}{|q|+\varepsilon}.
\]

传导率评价包含两类参考。第一类参考由真实速度场积分并换算得到，用于检验速度场积分链条是否一致；第二类参考来自教师传导率表，用于检验模型是否能够恢复 IPNM1 数据中的局部传导率标签。传导率相对误差为

\[
\mathrm{RE}_g
=
\frac{|\hat{g}-g|}{|g|+\varepsilon},
\]

传导率决定系数为

\[
R_g^2
=
1-
\frac{
\sum(\hat{g}_i-g_i)^2
}{
\sum(g_i-\bar{g})^2
}.
\]

主结果显示，Res-IPNM1-FlowNet 的孔隙像素速度场 \(R^2\) 为

\[
0.9875\pm0.0006,
\]

相对教师传导率表的平均误差为

\[
2.07\%\pm0.02\%,
\]

对应传导率 \(R^2\) 为

\[
0.9980\pm0.0001.
\]

这说明模型在保持速度场空间信息的同时，也能准确恢复 IPNM1 局部传导率。

## 与直接传导率拟合的区别

直接标量拟合可以写为

\[
\mathcal{G}(y,z)\rightarrow g.
\]

这种方法的优势是形式简单，但它将几何作用、速度分布、积分流量和传导率换算全部压缩到一个标量目标中。这样虽然可能在某些数据上拟合整体流量，但不能提供局部速度分布，也难以解释误差来自几何识别、速度预测还是传导率换算。

本文方法采用分解式路径：

\[
\mathcal{G}(y,z)
\rightarrow
\hat{u}(y,z)
\rightarrow
\hat{q}
\rightarrow
\hat{g}.
\]

这种分解使每一步都有明确物理含义。神经网络学习最困难的几何到速度场映射；流量和传导率则通过显式物理关系得到。因此，该方法不仅能输出孔网模型所需传导率，还能保留后续传质分析所需的速度分布。

实验结果也支持这一点。直接拟合真实积分流量可以达到较低误差，但无法给出速度场；直接拟合教师表格传导率则表现不稳定。相比之下，速度场优先的 Res-IPNM1-FlowNet 同时获得高速度场 \(R^2\) 和低传导率误差，说明物理中间量对该问题是必要且有效的。

## IPNM2 扩展边界

当前方法完成的是 IPNM1 局部传导率预测。若要进入 IPNM2，需要在局部传导率之外增加网络组织关系。设一个完整喉道由 \(K\) 个子喉道组成，各子喉道传导率为 \(g_k\)，则在串联近似下完整喉道传导率满足

\[
\frac{1}{g_{\mathrm{throat}}}
=
\sum_{k=1}^{K}
\frac{1}{g_k}.
\]

随后，\(g_{\mathrm{throat}}\) 需要被赋给孔网边，再求解整个孔网的节点压力、边流量和整体渗透响应。由于现有监督数据尚未完整提供子喉道到完整喉道再到网络边的映射关系，本文不声称完成 IPNM2，而是将 Res-IPNM1-FlowNet 定位为 IPNM2 扩展中的局部物理代理模块。

## 方法小结

本文方法以局部截面几何为输入，以速度场为核心预测对象，以 IPNM1 积分关系恢复流量和传导率。其逻辑可概括为：用统一岩样模型学习几何到速度场的复杂映射，再用物理积分和传导率换算完成孔网模型所需参数的恢复。

该设计避免了直接标量拟合丢失速度分布的问题，也避免了按岩样分类带来的额外建模复杂度。实验表明，适度的积分流量约束能够提升传导率一致性而不牺牲速度场精度。因此，Res-IPNM1-FlowNet 为 IPNM1 局部传导率预测提供了一种兼具物理解释性、速度场可用性和计算效率的代理建模方案。
